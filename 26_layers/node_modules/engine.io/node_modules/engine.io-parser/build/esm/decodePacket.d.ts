import { Packet, BinaryType, RawData } from "engine.io/node_modules/engine.io-parser/build/esm/commons";
export declare const decodePacket: (encodedPacket: RawData, binaryType?: BinaryType) => Packet;
